# UI V2 performance behavior

The visual density is intentionally restored on capable devices. The implementation still pauses the runtime timer while the tab is hidden, pauses particles when the document is not visible, lowers particle count on compact screens, and respects the user's reduced-motion preference.
